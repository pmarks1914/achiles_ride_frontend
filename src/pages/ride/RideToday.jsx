import React from 'react';
import ChatContainer from './ChatContainer';

const RideToday = () => {
    return (
        <div>
             <ChatContainer />
        </div>
    );
};

export default RideToday;